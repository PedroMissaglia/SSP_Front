extends layout

block content
  .login
    .login-box
      .login-logo
        .card.col-mb-4
          .card-body.login-card-body
            h2.login-box-msg Login
              h3 Super Secret Project
            br
            form(name ="loginForm" onsubmit="return validateForm()" method="post")
              .input-group.mb-4
                input.form-control(id="1" name ="email" type='email', placeholder='Email')
                .input-group-append
                  .input-group-text.font-size
                    span.fas.fa-envelope
              br      
              .input-group.mb-4
                input.form-control(id="2" name="senha" type='password', placeholder='Password')
                .input-group-append
                  .input-group-text
                    span.fas.fa-lock
              br      
              .row               
                .col-4
                  button.btn.btn-primary.a(type='submit') Sign In
                // /.col 
              br 
              p - OR -
            p.mb-0
              a.text-center(href='views/register.pug') Register a new membership
              button(class="btn btn-primary" onclick="getUser()") Testing
            div(id="particles-js")  
  canvas(class="background")